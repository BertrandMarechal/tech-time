// @ts-expect-error Need to update the config
export enum CookieNames {
  Frontend = "tt-fe",
  Backend = "tt-be",
}

// @ts-expect-error Need to update the config
export enum BackendType {
  Python = "python",
  Go = "go",
}

// @ts-expect-error Need to update the config
export enum FrontendType {
  React = "react",
  Vue = "vue",
}
